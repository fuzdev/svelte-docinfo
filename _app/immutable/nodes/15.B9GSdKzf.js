import"../chunks/DsnmJJEf.js";import{p as B,b as W,i as U,f as k,a as _,s as e,d as t,g as q,c as g,$ as V,n as o,r as s}from"../chunks/iYPbPufE.js";import{h as Y}from"../chunks/Bm5_blfa.js";import{C as x}from"../chunks/FdTAqBrw.js";import{T as Z,a as v,b as y}from"../chunks/bBFAaf_v.js";import{D as d}from"../chunks/B0C8H7r1.js";import{M as X}from"../chunks/Ck1zUBrh.js";import{T as D}from"../chunks/BBMkE1cu.js";import{c as G}from"../chunks/CC8l6xUA.js";var Q=g(`<!> <p><strong>Accumulated (non-fatal)</strong>: appended to the <!> array, analysis continues. Covers type resolution failures,
				member or prop extraction failures, and JSDoc tag misuse. The return value is still valid but
				may carry <code>partial: true</code> on affected declarations.</p> <p><strong>Thrown (fatal)</strong>: a small set of setup-level conditions throws from public
				entry points: missing <code>tsconfig.json</code>, Svelte &lt;5 detected, or <code>discovery: 'exports'</code> mode with no resolvable exports. Wrap the top-level <code>analyze</code> / <code>analyzeFromFiles</code> call if you want to handle these.
				svelte2tsx transformation failures are <em>not</em> thrown; they flow as <code>transform_failed</code> diagnostics.</p>`,1),ee=g(`<!> <p><code>diagnostics</code> is a plain <code>Array&lt;<!>&gt;</code>, no wrapper, no methods.
				Round-trips through <code>JSON.stringify</code> / <code>z.array(Diagnostic).parse</code>, so
				it serializes alongside <code>modules</code> in CLI output and rehydrates cleanly.</p> <!> <p>Each <!> carries:</p> <ul><li><code>kind</code>: discriminant, one per failure mode (see table below)</li> <li><code>severity</code>: <code>"error"</code> or <code>"warning"</code></li> <li><code>file</code>: POSIX-form, project-relative (no leading <code>./</code>). Rejoin with <code>projectRoot</code> for absolute paths — including a file outside the project, which
					takes the <code>../</code> form so the rejoin still recovers it. It names a <em>file</em>, not a module: <code>ModuleJson.path</code> is relative to <code>sourceRoot</code>, so <code>modules.find((m) =&gt; m.path === d.file)</code> is not a valid lookup (that idiom applies
					to module paths in printed type text, which are a different string).</li> <li><code>line</code>, <code>column</code>: 1-based, optional. Absent when there's no precise
					AST node (e.g., a module-level skip). Positions from a Svelte <code>&lt;script module&gt;</code> are remapped to the original <code>.svelte</code> source;
					an unmappable position is dropped rather than published as a line in the svelte2tsx output</li> <li><code>message</code>: human-readable description. Scrubbed of project-root paths and
					svelte2tsx virtual suffixes like <code>file</code>, so no diagnostic carries an absolute
					path.</li> <li>additional fields specific to the variant: <code>symbolName</code>, <code>className</code>, <code>tagName</code>, etc.</li></ul>`,1),oe=g(`<!> <p>Severity is stable per kind: every kind below is <code>warning</code> severity except <code>transform_failed</code> and <code>module_unreadable</code>, which are always <code>error</code>.</p> <table><thead><tr><th class="white-space:nowrap">kind</th><th>When it fires</th></tr></thead><tbody><tr><td><code>type_extraction_failed</code></td><td><strong>Trigger:</strong> type resolution threw on a symbol. <strong>Consequence:</strong> declaration included with <code>partial: true</code> and
							empty <code>typeSignature</code>.</td></tr><tr><td><code>signature_analysis_failed</code></td><td><strong>Trigger:</strong> function or method signature analysis threw, usually
							circular generics or unresolved call signatures. <strong>Consequence:</strong> declaration included with <code>partial: true</code>;
							parameters and overloads may be empty.</td></tr><tr><td><code>class_member_failed</code></td><td><strong>Trigger:</strong> one member of a class couldn't be analyzed. <strong>Consequence:</strong> member included with <code>partial: true</code>;
							siblings still extract normally.</td></tr><tr><td><code>svelte_prop_failed</code></td><td><strong>Trigger:</strong> a Svelte component prop type couldn't be resolved through
							the checker. <strong>Consequence:</strong> per-prop type resolution failures fall back
							to <code>"any"</code> for that prop with siblings unaffected; when the whole <code>$props&lt;T&gt;()</code> annotation type is itself unresolvable, the component's <code>props</code> array drops to empty.</td></tr><tr><td><code>legacy_props</code></td><td><strong>Trigger:</strong> a component declares props with legacy <code>export let</code> syntax — still-legal Svelte 5, but prop extraction anchors on <code>$props()</code>, so nothing was extracted. <code>propNames</code> lists the
							detected props. <strong>Consequence:</strong> the component emits with empty <code>props</code>; migrate to <code>$props()</code>.</td></tr><tr><td><code>module_skipped</code></td><td><strong>Trigger:</strong> whole module skipped during the analysis pass. <code>reason</code> narrows to <code>"not_in_program"</code>, <code>"no_analyzer"</code>, or <code>"requires_program"</code>. <strong>Consequence:</strong> module absent from <code>modules[]</code>.</td></tr><tr><td><code>module_unreadable</code></td><td><strong>Trigger:</strong> file named in <code>package.json</code> exports exists but <code>readFile</code> failed (permission denied, FS error). <strong>Consequence:</strong> file dropped from the discovered set. <strong>Discovery-time.</strong></td></tr><tr><td><code>import_parse_failed</code></td><td><strong>Trigger:</strong> import parsing failed during dependency resolution. <strong>Consequence:</strong> the dependency edge is dropped; the module itself still
							analyzes. <strong>Ingest-time.</strong></td></tr><tr><td><code>duplicate_comment</code></td><td><strong>Trigger:</strong> two sources supplied a comment for the same target (HTML <code>@component</code> + script JSDoc, or multiple <code>@module</code> comments). <code>commentType</code> narrows to <code>"module_comment"</code> or <code>"doc_comment"</code>. <strong>Consequence:</strong> the higher-priority source
							wins: JSDoc for doc comments; instance <code>&lt;script&gt;</code> &gt; <code>&lt;script module&gt;</code> &gt; HTML comment for module comments.</td></tr><tr><td><code>misplaced_tag</code></td><td><strong>Trigger:</strong> symbol-scope tag (<code>@example</code>, <code>@deprecated</code>, <code>@internal</code>, <code>@since</code>, <code>@see</code>, <code>@throws</code>, <code>@mutates</code>, <code>@default</code>, <code>@nodocs</code>) found on a non-primary overload
							signature, or <code>@nodocs</code> in a <code>@module</code> comment (where it has no meaning; <code>functionName</code> is absent in that case). <strong>Consequence:</strong> the tag is dropped; move it to the primary signature —
							or, to omit a module from analysis, use <code>exclude</code> patterns.</td></tr><tr><td><code>unknown_param</code></td><td><strong>Trigger:</strong> <code>@param</code> key didn't match any actual parameter
							(typo or stale doc after a rename). <strong>Consequence:</strong> the description is dropped.</td></tr><tr><td><code>alias_lost</code></td><td><strong>Trigger:</strong> an exported type alias's right-hand side (an indexed access
							or conditional — <code>z.infer&lt;typeof S&gt;</code> and friends) resolves to a type
							the checker keeps no name for, and nothing self-heals: losses the alias registry
							recovers are suppressed (their <code>typeInfo</code> emits <code>&#123;kind: 'reference', name&#125;</code> at use sites), as are literal-only
							unions (<code>z.enum</code> outputs) and brand intersections — readable degradations
							with no author-side fix. <code>aliasName</code> names the alias. <strong>Consequence:</strong> unannotated positions document the alias's structure
							instead of its name; where applicable, an author-side nominal symbol (<code>interface Foo extends z.infer&lt;typeof S&gt; &#123;&#125;</code>) restores it.</td></tr><tr><td><code>duplicate_declaration</code></td><td><strong>Trigger:</strong> a declaration name appears in more than one module, so the
							flat-namespace assumption collides. <code>declarationName</code> and <code>modules</code> name the conflict — <code>modules</code> holds <code>ModuleJson.path</code> values, so it is on a different base than this record's <code>file</code> and the two read differently for the same module. <strong>Consequence:</strong> always emitted; <code>onDuplicates</code> only controls whether to additionally throw, log, or invoke a
							callback.</td></tr><tr><td><code>transform_failed</code></td><td><strong>Trigger:</strong> svelte2tsx threw on a <code>.svelte</code> file. <strong>Consequence:</strong> the file's <!> is synthesized as a placeholder (<code>partial: true</code>, empty <code>declarations</code>). <strong>Ingest-time.</strong></td></tr><tr><td><code>source_map_failed</code></td><td><strong>Trigger:</strong> source map parsing failed for a Svelte virtual file. <strong>Consequence:</strong> analysis continues without position mapping — query-time
							diagnostics drop <code>line</code>/<code>column</code> rather than point into the
							generated TS, while declaration <code>sourceLine</code>s fall back to virtual
							positions. <strong>Ingest-time.</strong></td></tr><tr><td><code>resolver_failed</code></td><td><strong>Trigger:</strong> import resolver threw on a specifier (vs. legitimately
							returning <code>null</code> for externals). <code>specifier</code> names the failing
							import. <strong>Consequence:</strong> the dependency edge is dropped. <strong>Ingest-time.</strong></td></tr></tbody></table> <p>Each variant is a strict Zod object with its own extra fields. Use <!> to narrow to a specific variant for typed access:</p> <!>`,1),ne=g(`<!> <p><code>severity</code> says how loud to be about a problem; <code>partial: true</code> says a specific declaration or member has incomplete data,
				typically from <code>type_extraction_failed</code>, <code>signature_analysis_failed</code>, <code>class_member_failed</code>, or <code>svelte_prop_failed</code>. Branch on <code>partial</code> directly; no need to cross-reference diagnostics by file and line.</p>`,1),te=g(`<!> <p>The diagnostics array is a plain <code>Array&lt;<!>&gt;</code>: construct with <code>[]</code> and mutate with <code>Array.push</code>. Read helpers:</p> <ul><li><!>, <!>: boolean
					checks by severity</li> <li><!>, <!>: filter by
					severity</li> <li><!>: filter by kind, narrowed to the matching variant</li> <li><!>: format as <code>'./file.ts:10:5: error: message'</code> (the <code>'./'</code> prefix is fixed)</li></ul>`,1),se=g(`<!> <p>The <!> emits the structured <code>diagnostics</code> field
				alongside <code>modules</code> in JSON output — stripped from the wire when empty, so parse
				through <!> to restore the default <code>[]</code>. Progress messages go to stderr; the structured diagnostics appear only in
				the JSON:</p> <!> <p>Programmatically:</p> <!> <p>The <!>'s virtual module exports both <code>modules</code> and <code>diagnostics</code>, so SvelteKit apps can render a
				doc-warnings page without re-running analysis:</p> <!>`,1),ae=g(`<!> <p>Optional scalar fields (<code>line</code>, <code>column</code>) drop on serialize per the
				same compact-output rules as the rest of the schema. See <!> for
				the full rule. The Vite plugin's virtual module exposes <code>modules</code> and <code>diagnostics</code> as separate ES module exports, so they're always present even when
				empty. See <!> for the Zod schemas
				and helper signatures.</p>`,1),re=g(`<section><p>Analysis accumulates errors and warnings without halting. A failing declaration is marked <code>partial: true</code> and the rest of the module still analyzes. Detail lands in an array
			of <!> entries, alongside <code>modules</code> in the result.</p> <!> <!> <!> <!> <!> <!> <!></section>`);function ke(R,E){B(E,!0);const F=G("diagnostics");Y("1oyat2l",z=>{U(()=>{V.title="diagnostics - svelte-docinfo"})}),Z(R,{get tome(){return F},children:(z,ie)=>{var T=re(),S=t(T),L=e(t(S),3);d(L,{name:"Diagnostic"}),o(3),s(S);var N=e(S,2);v(N,{children:(p,w)=>{var a=Q(),r=k(a);y(r,{text:"Two-tier error model"});var n=e(r,2),c=e(t(n),2);d(c,{name:"Diagnostic"}),o(3),s(n),o(2),_(p,a)},$$slots:{default:!0}});var P=e(N,2);v(P,{children:(p,w)=>{var a=ee(),r=k(a);y(r,{text:"Shape"});var n=e(r,2),c=e(t(n),2),m=e(t(c));d(m,{name:"Diagnostic"}),o(),s(c),o(7),s(n);var l=e(n,2);x(l,{lang:"ts",dangerous_raw_html:`<span class="token_punctuation">{</span>
  modules<span class="token_operator">:</span> <span class="token_capitalized_identifier token_class_name">ModuleJson</span><span class="token_punctuation">[],</span>
  diagnostics<span class="token_operator">:</span> <span class="token_capitalized_identifier token_class_name">Diagnostic</span><span class="token_punctuation">[]</span>
<span class="token_punctuation">}</span>`});var u=e(l,2),i=e(t(u));d(i,{name:"Diagnostic"}),o(),s(u),o(2),_(p,a)},$$slots:{default:!0}});var A=e(P,2);v(A,{children:(p,w)=>{var a=oe(),r=k(a);y(r,{text:"Diagnostic kinds"});var n=e(r,4),c=e(t(n)),m=e(t(c),13),l=e(t(m)),u=e(t(l),6);d(u,{name:"ModuleJson"}),o(6),s(l),s(m),o(2),s(c),s(n);var i=e(n,2),$=e(t(i));d($,{name:"byKind"}),o(),s(i);var h=e(i,2);x(h,{lang:"ts",dangerous_raw_html:`<span class="token_special_keyword">import</span> <span class="token_punctuation">{</span>byKind<span class="token_punctuation">}</span> <span class="token_special_keyword">from</span> <span class="token_string">'svelte-docinfo'</span><span class="token_punctuation">;</span>

<span class="token_special_keyword">for</span> <span class="token_punctuation">(</span><span class="token_keyword">const</span> d <span class="token_keyword">of</span> <span class="token_function">byKind</span><span class="token_punctuation">(</span>diagnostics<span class="token_punctuation">,</span> <span class="token_string">'misplaced_tag'</span><span class="token_punctuation">))</span> <span class="token_punctuation">{</span>
  <span class="token_comment">// d.tagName, d.functionName, d.file, d.line are typed</span>
  <span class="token_builtin">console</span><span class="token_punctuation">.</span><span class="token_function">warn</span><span class="token_punctuation">(</span><span class="token_template_string"><span class="token_template_punctuation token_string">\`</span><span class="token_interpolation"><span class="token_interpolation_punctuation token_punctuation">\${</span>d<span class="token_punctuation">.</span>functionName<span class="token_interpolation_punctuation token_punctuation">}</span></span><span class="token_string">: move @</span><span class="token_interpolation"><span class="token_interpolation_punctuation token_punctuation">\${</span>d<span class="token_punctuation">.</span>tagName<span class="token_interpolation_punctuation token_punctuation">}</span></span><span class="token_string"> to the primary overload</span><span class="token_template_punctuation token_string">\`</span></span><span class="token_punctuation">);</span>
<span class="token_punctuation">}</span>`}),_(p,a)},$$slots:{default:!0}});var I=e(A,2);v(I,{children:(p,w)=>{var a=ne(),r=k(a);y(r,{text:"severity vs partial"}),o(2),_(p,a)},$$slots:{default:!0}});var j=e(I,2);v(j,{children:(p,w)=>{var a=te(),r=k(a);y(r,{text:"Helpers"});var n=e(r,2),c=e(t(n)),m=e(t(c));d(m,{name:"Diagnostic"}),o(),s(c),o(5),s(n);var l=e(n,2),u=t(l),i=t(u);d(i,{name:"hasErrors"});var $=e(i,2);d($,{name:"hasWarnings"}),o(),s(u);var h=e(u,2),b=t(h);d(b,{name:"errorsOf"});var C=e(b,2);d(C,{name:"warningsOf"}),o(),s(h);var f=e(h,2),K=t(f);d(K,{name:"byKind"}),o(),s(f);var J=e(f,2),H=t(J);d(H,{name:"formatDiagnostic"}),o(5),s(J),s(l),_(p,a)},$$slots:{default:!0}});var M=e(j,2);v(M,{children:(p,w)=>{var a=se(),r=k(a);y(r,{text:"Consuming diagnostics"});var n=e(r,2),c=e(t(n));D(c,{slug:"cli",children:(b,C)=>{o();var f=q("CLI");_(b,f)},$$slots:{default:!0}});var m=e(c,6);d(m,{name:"AnalyzeResultJson"}),o(3),s(n);var l=e(n,2);x(l,{lang:"bash",dangerous_raw_html:`npx svelte-docinfo <span class="token_operator">|</span> jq <span class="token_string">'.diagnostics | group_by(.kind) | map({kind: .[0].kind, count: length})'</span>`});var u=e(l,4);x(u,{lang:"ts",dangerous_raw_html:`<span class="token_special_keyword">import</span> <span class="token_punctuation">{</span>analyzeFromFiles<span class="token_punctuation">,</span> errorsOf<span class="token_punctuation">,</span> formatDiagnostic<span class="token_punctuation">,</span> byKind<span class="token_punctuation">}</span> <span class="token_special_keyword">from</span> <span class="token_string">'svelte-docinfo'</span><span class="token_punctuation">;</span>

<span class="token_keyword">const</span> <span class="token_punctuation">{</span>modules<span class="token_punctuation">,</span> diagnostics<span class="token_punctuation">}</span> <span class="token_operator">=</span> <span class="token_special_keyword">await</span> <span class="token_function">analyzeFromFiles</span><span class="token_punctuation">({</span>projectRoot<span class="token_operator">:</span> process<span class="token_punctuation">.</span><span class="token_function">cwd</span><span class="token_punctuation">()});</span>

<span class="token_comment">// File paths in diagnostics are already project-relative.</span>
<span class="token_special_keyword">for</span> <span class="token_punctuation">(</span><span class="token_keyword">const</span> d <span class="token_keyword">of</span> <span class="token_function">errorsOf</span><span class="token_punctuation">(</span>diagnostics<span class="token_punctuation">))</span> <span class="token_punctuation">{</span>
  <span class="token_builtin">console</span><span class="token_punctuation">.</span><span class="token_function">error</span><span class="token_punctuation">(</span><span class="token_function">formatDiagnostic</span><span class="token_punctuation">(</span>d<span class="token_punctuation">));</span>
<span class="token_punctuation">}</span>

<span class="token_comment">// Specific check: any @param typos?</span>
<span class="token_keyword">const</span> stale <span class="token_operator">=</span> <span class="token_function">byKind</span><span class="token_punctuation">(</span>diagnostics<span class="token_punctuation">,</span> <span class="token_string">'unknown_param'</span><span class="token_punctuation">);</span>
<span class="token_special_keyword">if</span> <span class="token_punctuation">(</span>stale<span class="token_punctuation">.</span>length<span class="token_punctuation">)</span> <span class="token_punctuation">{</span>
  <span class="token_builtin">console</span><span class="token_punctuation">.</span><span class="token_function">warn</span><span class="token_punctuation">(</span><span class="token_template_string"><span class="token_template_punctuation token_string">\`</span><span class="token_interpolation"><span class="token_interpolation_punctuation token_punctuation">\${</span>stale<span class="token_punctuation">.</span>length<span class="token_interpolation_punctuation token_punctuation">}</span></span><span class="token_string"> stale @param tag(s); fix or remove</span><span class="token_template_punctuation token_string">\`</span></span><span class="token_punctuation">);</span>
<span class="token_punctuation">}</span>`});var i=e(u,2),$=e(t(i));D($,{slug:"vite-plugin",children:(b,C)=>{o();var f=q("Vite plugin");_(b,f)},$$slots:{default:!0}}),o(5),s(i);var h=e(i,2);x(h,{lang:"ts",dangerous_raw_html:`<span class="token_special_keyword">import</span> <span class="token_punctuation">{</span>modules<span class="token_punctuation">,</span> diagnostics<span class="token_punctuation">}</span> <span class="token_special_keyword">from</span> <span class="token_string">'virtual:svelte-docinfo'</span><span class="token_punctuation">;</span>
<span class="token_special_keyword">import</span> <span class="token_punctuation">{</span>hasErrors<span class="token_punctuation">}</span> <span class="token_special_keyword">from</span> <span class="token_string">'svelte-docinfo'</span><span class="token_punctuation">;</span>

<span class="token_special_keyword">if</span> <span class="token_punctuation">(</span><span class="token_function">hasErrors</span><span class="token_punctuation">(</span>diagnostics<span class="token_punctuation">))</span> <span class="token_punctuation">{</span>
  <span class="token_comment">// surface in the UI or fail the build</span>
<span class="token_punctuation">}</span>`}),_(p,a)},$$slots:{default:!0}});var O=e(M,2);v(O,{children:(p,w)=>{var a=ae(),r=k(a);y(r,{text:"Absence rule"});var n=e(r,2),c=e(t(n),5);D(c,{slug:"output-format"});var m=e(c,6);X(m,{module_path:"diagnostics.ts",children:(l,u)=>{o();var i=q("diagnostics.ts");_(l,i)},$$slots:{default:!0}}),o(),s(n),_(p,a)},$$slots:{default:!0}}),s(T),_(z,T)},$$slots:{default:!0}}),W()}export{ke as component};
