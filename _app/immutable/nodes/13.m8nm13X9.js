import"../chunks/DsnmJJEf.js";import{p as H,b as K,i as Q,f as h,g as $,a as d,s as e,d as p,c as k,$ as Z,n as o,r}from"../chunks/iYPbPufE.js";import{h as ee}from"../chunks/Bm5_blfa.js";import{C as g}from"../chunks/FdTAqBrw.js";import{T as se,a as f,b as v}from"../chunks/bBFAaf_v.js";import{D as u}from"../chunks/B0C8H7r1.js";import{M as j}from"../chunks/Ck1zUBrh.js";import{T as x}from"../chunks/BBMkE1cu.js";import{c as ne}from"../chunks/CC8l6xUA.js";var ae=k(`<!> <p>Every analysis entry point, including <!>, <!>'s <code>setFile</code> / <code>setFiles</code>, and even <!>'s output,
			operates on <!>:</p> <!> <ul><li><strong><code>id</code></strong>: absolute path. Native paths are accepted at the public-API
				boundary and posixified at ingest (Windows backslash paths become forward-slash internally).
				After ingest, every comparison and output field uses POSIX form.</li> <li><strong><code>content</code></strong>: required, since analysis functions don't read from
				disk. Files may come from any source: filesystem, build pipeline, in-memory editor buffer.</li> <li><strong><code>dependencies</code></strong>: opt-in. When supplied, the session skips its
				lex+resolve pass for this entry and treats the array as authoritative. See "Pre-resolved
				dependencies" below.</li></ul> <p>Reverse edges (<code>dependents</code>) are computed inside the analyzer from forward edges,
			never caller-supplied.</p>`,1),oe=k(`<!> <p>Pick by what your tool already owns:</p> <table><thead><tr><th class="white-space:nowrap">Use</th><th>When</th></tr></thead><tbody><tr><td><!></td><td>You own a project root and want everything in one call: file discovery, dependency
						resolution, analysis. CLI-style.</td></tr><tr><td><!></td><td>You already hold file contents in memory (a Rollup/esbuild plugin with files in the
						bundle graph). Single-pass, one-shot.</td></tr><tr><td><!></td><td>You re-analyze the same source set across many cycles (Vite, watch mode, LSP). See the <!> guide.</td></tr></tbody></table> <p>The one-shot APIs are thin wrappers over single-use sessions. They share the same two-phase
			analysis loop, same diagnostic model, same output shape.</p>`,1),te=k(`<!> <p><!> runs file discovery without analysis. Useful when
			your tool wants to know the source set up front (for watcher-glob registration, count displays,
			pre-flight checks) but defers analysis to a separate pass.</p> <!> <p>Discovery strategies match the CLI and Vite plugin: <code>'auto'</code> tries package.json
			exports first and falls back to glob; <code>'exports'</code> is strict (throws if exports is
			missing); <code>'glob'</code> skips exports entirely. Providing <code>include</code> under <code>'auto'</code> collapses the chain to glob immediately, since
			honoring it from exports discovery would silently drop the user's filter on packages with an <code>exports</code> field.</p> <p><code>module_unreadable</code> is the discovery-time diagnostic; the session doesn't run
			discovery, so direct session consumers own it. <code>analyzeFromFiles</code> merges discovery
			diagnostics into the final return before handing back to the caller. Discovery is also the one
			diagnostic source that doesn't normalize its own paths — its <code>message</code> wraps the fs
			error, which embeds an absolute path — so call <!> on them before publishing anywhere a developer's
			filesystem shouldn't go.</p>`,1),pe=k(`<!> <p>Build tools that already maintain a dependency graph (Gro's filer, Rollup's bundle graph,
			webpack's module graph) can hand it over by populating <code>SourceFileInfo.dependencies</code> with absolute paths. The session then skips its lex+resolve
			pass for that entry entirely.</p> <!> <p>The cache key for the entry shifts to <code>(content, dependencies-element-wise-equal)</code> instead of <code>(content, resolverIdentity)</code>. A fresh array per call cache-hits cleanly as long as
			the contents match, with no upstream memoization needed. The session snapshots the array at
			ingest, so mid-flight mutation of the caller's array won't produce false hits.</p>`,1),re=k(`<!> <p>The pre-resolved path is trust mode: the session does not cross-check declared dependencies
			against the file's <code>content</code>. Two consequences a buggy resolver upstream will hit
			silently:</p> <ul><li>Edges declared in <code>dependencies</code> but absent from <code>content</code> are accepted
				as-is.</li> <li>Edges present in <code>content</code> but missing from <code>dependencies</code> are silently
				omitted.</li></ul> <p>There's no diagnostic for either case, because legitimate cross-batch sequences
			(declare-then-set, declare-then-delete) would produce noise. The lex+resolve fallback path has
			no such hole; its edges are always grounded in syntactic imports. If you don't fully trust the
			dependency source you're handing in, use lex+resolve and pay the parse cost.</p>`,1),ce=k(`<!> <p>Whether <code></code> shows up as a dependency is the caller's decision
			under the pre-resolved path. Two common stances:</p> <ul><li><strong>Keep type-only edges</strong>: what default lex+resolve does (<code>es-module-lexer</code> doesn't distinguish them). The output's <code>ModuleJson.dependencies</code> matches the syntactic
				import set.</li> <li><strong>Drop type-only edges</strong>: what Gro's filer (<code>parse_imports</code> with <code>ignore_types=true</code>) does. Type imports aren't runtime deps; the output reflects
				runtime graph only.</li></ul> <p>Both are valid; the pre-resolved path defers the policy to the caller. Switching from
			lex+resolve to pre-resolved-via-Gro will visibly remove type-only edges from <code>ModuleJson.dependencies</code> / <code>dependents</code> for affected modules.</p>`,1),le=k(`<!> <p>When you don't hand over pre-resolved dependencies, the session lexes specifiers from <code>content</code> and passes them through an <!>. Build-tool integrations typically want to wire
			their own resolver in so module resolution matches what the rest of the build does:</p> <!> <p><code>identity</code> is the stable cache token. See the <!> guide for why
			it's required and how to choose one. When omitted entirely, the session lazily constructs a TS +
			tsconfig default on first use — reusing the compiler options its <code>LanguageService</code> already parsed — but only if at least one file in any batch lacks <code>dependencies</code>. Fully pre-resolved batches never trigger the default at all.</p>`,1),ie=k(`<!> <p><!> builds the <!> the session needs. Customize for non-default project
			layouts:</p> <!> <p><code>sourceRoot</code> controls module-path stripping in <code>ModuleJson.path</code>: pass <code>'.'</code> for project-relative paths. <code>exclude</code> is the single source of truth for user
			exclusions, applied at both discovery and analysis time, so a file dropped here never shows up
			in the output regardless of how it was discovered. The defaults cover tests and the <code>src/lib/internal/</code> convention (<code>**/internal/**</code> — internal modules ship
			for public modules to import but aren't documented); an array override replaces them
			wholesale, while the callback form extends them without restating them. Beneath both sits an
			always-on baseline that <code>exclude</code> overrides can't strip: <code>node_modules</code> and dot-directories
			below a matched source path are never source. The baseline matches relative to the matched
			source path, so an explicit dot-directory source path (<code>sourcePaths: ['.hidden/src']</code>) still works — that relativity is the opt-out.</p> <p><code>sourcePaths</code> entries and <code>sourceRoot</code> are projectRoot-relative;
			absolute entries inside the root are accepted and stored root-relative, while an entry
			resolving outside the root throws at options creation — out-of-root modules are
			unrepresentable, since <code>ModuleJson.path</code> is <code>sourceRoot</code>-relative and <code>Diagnostic.file</code> project-root-relative, and neither can reach outside the root.
			The same accept-or-throw rule covers absolute <code>exclude</code> globs (and <code>include</code> patterns at the discovery entry points), so
			discovery and analysis always see the same relative pattern.</p>`,1),de=k(`<!> <p>Every path stored, compared, or used as a Map/Set key inside the analyzer is POSIX form
			(forward slashes). Native paths are accepted at the public-API boundary and posixified at
			ingest, so Windows callers can hand in <code>C:\\\\repo\\\\src\\\\lib\\\\foo.ts</code> without thinking about it, but the resulting <code>ModuleJson.path</code>, <code>Diagnostic.file</code>, and session <code>list()</code> output report POSIX form.</p> <p>Out of scope: drive-letter case normalization (<code>C:\\\\</code> vs <code>c:\\\\</code>) and
			Windows extended-length <code>\\\\\\\\?\\\\</code> prefixes. See <!> for the chokepoint implementation.</p>`,1),ue=k(`<!> <p>Two bounded-concurrency caps protect against runaway parallelism in the integration paths:</p> <ul><li><code>MAX_FILE_CONCURRENCY</code> caps parallel <code>readFile</code> calls (used by <code>files.globFiles</code>, <code>exports.discoverFromExports</code>).</li> <li><code>MAX_RESOLVE_CONCURRENCY</code> caps parallel resolver calls (used by the session's phase-2
				lex+resolve pass).</li></ul> <p>Same numerical value today, named separately for future independent tuning. Both run through a
			fail-fast, order-preserving worker pool (<code>map_concurrent</code> in <!>).</p>`,1),_e=k(`<section><p>svelte-docinfo is build-tool agnostic. Files come in through <!> objects (absolute path plus content, optionally with pre-resolved
			dependencies), and the analyzer never touches disk on its own. This page covers the integration
			surface for embedding analysis inside a bundler, watcher, or LSP-style tool that doesn't fit the
			bundled <!>.</p></section> <!> <!> <!> <!> <!> <!> <!> <!> <!> <!>`,1);function Se(B,X){H(X,!0);const Y=ne("build-tools");ee("1h4iot3",S=>{Q(()=>{Z.title="build-tool integration - svelte-docinfo"})}),se(B,{get tome(){return Y},children:(S,ke)=>{var P=_e(),w=h(P),F=p(w),I=e(p(F));u(I,{name:"SourceFileInfo"});var q=e(I,2);x(q,{slug:"vite-plugin",children:(t,m)=>{o();var s=$("Vite plugin");d(t,s)},$$slots:{default:!0}}),o(),r(F),r(w);var T=e(w,2);f(T,{children:(t,m)=>{var s=ae(),a=h(s);v(a,{text:"SourceFileInfo"});var n=e(a,2),c=e(p(n));u(c,{name:"analyze"});var l=e(c,2);u(l,{name:"createAnalysisSession"});var i=e(l,6);u(i,{name:"discoverSourceFiles"});var _=e(i,2);u(_,{name:"SourceFileInfo"}),o(),r(n);var y=e(n,2);g(y,{lang:"ts",dangerous_raw_html:`<span class="token_keyword">interface</span> <span class="token_class_name">SourceFileInfo</span> <span class="token_punctuation">{</span>
  id<span class="token_operator">:</span> <span class="token_builtin">string</span><span class="token_punctuation">;</span>                  <span class="token_comment">// absolute path (native ok at boundary)</span>
  content<span class="token_operator">:</span> <span class="token_builtin">string</span><span class="token_punctuation">;</span>             <span class="token_comment">// file contents</span>
  dependencies<span class="token_operator">?:</span> <span class="token_builtin">string</span><span class="token_punctuation">[];</span>     <span class="token_comment">// optional: pre-resolved deps (opt-in)</span>
<span class="token_punctuation">}</span>`}),o(4),d(t,s)},$$slots:{default:!0}});var O=e(T,2);f(O,{children:(t,m)=>{var s=oe(),a=h(s);v(a,{text:"Three entry points by ownership"});var n=e(a,4),c=e(p(n)),l=p(c),i=p(l),_=p(i);u(_,{name:"analyzeFromFiles"}),r(i),o(),r(l);var y=e(l),D=p(y),U=p(D);u(U,{name:"analyze"}),r(D),o(),r(y);var N=e(y),b=p(N),V=p(b);u(V,{name:"createAnalysisSession"}),r(b);var W=e(b),G=e(p(W));x(G,{slug:"session"}),o(),r(W),r(N),r(c),r(n),o(2),d(t,s)},$$slots:{default:!0}});var R=e(O,2);f(R,{children:(t,m)=>{var s=te(),a=h(s);v(a,{text:"discoverSourceFiles (standalone)"});var n=e(a,2),c=p(n);u(c,{name:"discoverSourceFiles"}),o(),r(n);var l=e(n,2);g(l,{lang:"ts",dangerous_raw_html:`<span class="token_special_keyword">import</span> <span class="token_punctuation">{</span>discoverSourceFiles<span class="token_punctuation">,</span> createSourceOptions<span class="token_punctuation">}</span> <span class="token_special_keyword">from</span> <span class="token_string">'svelte-docinfo'</span><span class="token_punctuation">;</span>

<span class="token_keyword">const</span> <span class="token_punctuation">{</span>files<span class="token_punctuation">,</span> diagnostics<span class="token_punctuation">}</span> <span class="token_operator">=</span> <span class="token_special_keyword">await</span> <span class="token_function">discoverSourceFiles</span><span class="token_punctuation">({</span>
  sourceOptions<span class="token_operator">:</span> <span class="token_function">createSourceOptions</span><span class="token_punctuation">(</span>process<span class="token_punctuation">.</span><span class="token_function">cwd</span><span class="token_punctuation">()),</span>
  <span class="token_comment">// discovery: 'auto' | 'exports' | 'glob'  (default 'auto')</span>
  <span class="token_comment">// include: ['src/**/*.ts']                (forces glob under 'auto')</span>
  <span class="token_comment">// distDir: 'dist'                         (for exports discovery)</span>
<span class="token_punctuation">});</span>

<span class="token_comment">// files: Array&lt;SourceFileInfo> — content already loaded from disk</span>
<span class="token_comment">// diagnostics: module_unreadable for any file the exports map names</span>
<span class="token_comment">//   but readFile failed on (permission denied, FS error)</span>`});var i=e(l,4),_=e(p(i),6);u(_,{name:"normalizeDiagnosticPaths"}),o(),r(i),d(t,s)},$$slots:{default:!0}});var A=e(R,2);f(A,{children:(t,m)=>{var s=pe(),a=h(s);v(a,{text:"Pre-resolved dependencies (fast path)"});var n=e(a,4);g(n,{lang:"ts",dangerous_raw_html:`<span class="token_special_keyword">import</span> <span class="token_import_type_keyword token_special_keyword">type</span> <span class="token_punctuation">{</span><span class="token_capitalized_identifier token_class_name">SourceFileInfo</span><span class="token_punctuation">}</span> <span class="token_special_keyword">from</span> <span class="token_string">'svelte-docinfo'</span><span class="token_punctuation">;</span>

<span class="token_comment">// inside your build-tool integration</span>
<span class="token_keyword">const</span> files<span class="token_type_annotation"><span class="token_:">:</span> <span class="token_type"><span class="token_type_name token_class_name">Array</span><span class="token_operator">&lt;</span><span class="token_type_name token_class_name">SourceFileInfo</span><span class="token_operator">></span></span></span> <span class="token_operator">=</span> <span class="token_punctuation">[</span><span class="token_operator">...</span>filer<span class="token_punctuation">.</span>modules<span class="token_punctuation">].</span><span class="token_function">map</span><span class="token_punctuation">(([</span>id<span class="token_punctuation">,</span> mod<span class="token_punctuation">])</span> <span class="token_operator">=></span> <span class="token_punctuation">({</span>
  id<span class="token_punctuation">,</span>
  content<span class="token_operator">:</span> mod<span class="token_punctuation">.</span>content<span class="token_punctuation">,</span>
  dependencies<span class="token_operator">:</span> <span class="token_punctuation">[</span><span class="token_operator">...</span>mod<span class="token_punctuation">.</span>dependencies<span class="token_punctuation">.</span><span class="token_function">keys</span><span class="token_punctuation">()],</span>  <span class="token_comment">// absolute paths</span>
<span class="token_punctuation">}));</span>

<span class="token_special_keyword">await</span> session<span class="token_punctuation">.</span><span class="token_function">setFiles</span><span class="token_punctuation">(</span>files<span class="token_punctuation">);</span>`}),o(2),d(t,s)},$$slots:{default:!0}});var z=e(A,2);f(z,{children:(t,m)=>{var s=re(),a=h(s);v(a,{text:"Trust contract"}),o(6),d(t,s)},$$slots:{default:!0}});var C=e(z,2);f(C,{children:(t,m)=>{var s=ce(),a=h(s);v(a,{text:"Type-only edges"});var n=e(a,2),c=e(p(n));c.textContent="import type {X} from './x'",o(),r(n),o(4),d(t,s)},$$slots:{default:!0}});var M=e(C,2);f(M,{children:(t,m)=>{var s=le(),a=h(s);v(a,{text:"ImportResolver (lex+resolve path)"});var n=e(a,2),c=e(p(n),3);u(c,{name:"ImportResolver"}),o(),r(n);var l=e(n,2);g(l,{lang:"ts",dangerous_raw_html:`<span class="token_special_keyword">import</span> <span class="token_punctuation">{</span>createAnalysisSession<span class="token_punctuation">,</span> createSourceOptions<span class="token_punctuation">}</span> <span class="token_special_keyword">from</span> <span class="token_string">'svelte-docinfo'</span><span class="token_punctuation">;</span>
<span class="token_special_keyword">import</span> <span class="token_import_type_keyword token_special_keyword">type</span> <span class="token_punctuation">{</span><span class="token_capitalized_identifier token_class_name">ImportResolver</span><span class="token_punctuation">}</span> <span class="token_special_keyword">from</span> <span class="token_string">'svelte-docinfo'</span><span class="token_punctuation">;</span>

<span class="token_keyword">const</span> resolver<span class="token_type_annotation"><span class="token_:">:</span> <span class="token_type"><span class="token_type_name token_class_name">ImportResolver</span></span></span> <span class="token_operator">=</span> <span class="token_punctuation">{</span>
  identity<span class="token_operator">:</span> <span class="token_string">'my-bundler@v1'</span><span class="token_punctuation">,</span>
  <span class="token_function_variable token_function">resolve</span><span class="token_operator">:</span> <span class="token_punctuation">(</span>specifier<span class="token_punctuation">,</span> fromFile<span class="token_punctuation">)</span> <span class="token_operator">=></span> bundler<span class="token_punctuation">.</span><span class="token_function">resolve</span><span class="token_punctuation">(</span>specifier<span class="token_punctuation">,</span> fromFile<span class="token_punctuation">),</span>
<span class="token_punctuation">};</span>

<span class="token_keyword">const</span> session <span class="token_operator">=</span> <span class="token_function">createAnalysisSession</span><span class="token_punctuation">({</span>
  sourceOptions<span class="token_operator">:</span> <span class="token_function">createSourceOptions</span><span class="token_punctuation">(</span>process<span class="token_punctuation">.</span><span class="token_function">cwd</span><span class="token_punctuation">()),</span>
  resolveImport<span class="token_operator">:</span> resolver<span class="token_punctuation">,</span>
<span class="token_punctuation">});</span>`});var i=e(l,2),_=e(p(i),2);x(_,{slug:"session"}),o(5),r(i),d(t,s)},$$slots:{default:!0}});var E=e(M,2);f(E,{children:(t,m)=>{var s=ie(),a=h(s);v(a,{text:"Source options and the source root"});var n=e(a,2),c=p(n);u(c,{name:"createSourceOptions"});var l=e(c,2);u(l,{name:"ModuleSourceOptions"}),o(),r(n);var i=e(n,2);g(i,{lang:"ts",dangerous_raw_html:`<span class="token_special_keyword">import</span> <span class="token_punctuation">{</span>createSourceOptions<span class="token_punctuation">}</span> <span class="token_special_keyword">from</span> <span class="token_string">'svelte-docinfo'</span><span class="token_punctuation">;</span>

<span class="token_comment">// Default: single sourcePaths=['src/lib'], test/spec + internal/ exclude.</span>
<span class="token_keyword">const</span> opts <span class="token_operator">=</span> <span class="token_function">createSourceOptions</span><span class="token_punctuation">(</span>process<span class="token_punctuation">.</span><span class="token_function">cwd</span><span class="token_punctuation">());</span>

<span class="token_comment">// Monorepo: multiple source directories, optional explicit sourceRoot.</span>
<span class="token_keyword">const</span> monorepoOpts <span class="token_operator">=</span> <span class="token_function">createSourceOptions</span><span class="token_punctuation">(</span>process<span class="token_punctuation">.</span><span class="token_function">cwd</span><span class="token_punctuation">(),</span> <span class="token_punctuation">{</span>
  sourcePaths<span class="token_operator">:</span> <span class="token_punctuation">[</span><span class="token_string">'packages/a/src'</span><span class="token_punctuation">,</span> <span class="token_string">'packages/b/src'</span><span class="token_punctuation">],</span>
  <span class="token_comment">// sourceRoot derived as longest common prefix when omitted.</span>
<span class="token_punctuation">});</span>

<span class="token_comment">// Extend the default exclude (callback form — the array form replaces).</span>
<span class="token_keyword">const</span> customOpts <span class="token_operator">=</span> <span class="token_function">createSourceOptions</span><span class="token_punctuation">(</span>process<span class="token_punctuation">.</span><span class="token_function">cwd</span><span class="token_punctuation">(),</span> <span class="token_punctuation">{</span>
  <span class="token_function_variable token_function">exclude</span><span class="token_operator">:</span> <span class="token_punctuation">(</span>defaults<span class="token_punctuation">)</span> <span class="token_operator">=></span> <span class="token_punctuation">[</span><span class="token_operator">...</span>defaults<span class="token_punctuation">,</span> <span class="token_string">'**/fixtures/**'</span><span class="token_punctuation">],</span>
<span class="token_punctuation">});</span>`}),o(4),d(t,s)},$$slots:{default:!0}});var L=e(E,2);f(L,{children:(t,m)=>{var s=de(),a=h(s);v(a,{text:"Paths: POSIX-form contract"});var n=e(a,4),c=e(p(n),7);j(c,{module_path:"paths.ts",children:(l,i)=>{o();var _=$("paths.ts");d(l,_)},$$slots:{default:!0}}),o(),r(n),d(t,s)},$$slots:{default:!0}});var J=e(L,2);f(J,{children:(t,m)=>{var s=ue(),a=h(s);v(a,{text:"Concurrency caps"});var n=e(a,6),c=e(p(n),3);j(c,{module_path:"concurrency.ts",children:(l,i)=>{o();var _=$("concurrency.ts");d(l,_)},$$slots:{default:!0}}),o(),r(n),d(t,s)},$$slots:{default:!0}}),d(S,P)},$$slots:{default:!0}}),K()}export{Se as component};
