import"../chunks/DsnmJJEf.js";import{p as ee,b as te,i as oe,g as S,a as c,f as u,d as a,s as e,c as p,$ as ae,n as r,r as n}from"../chunks/iYPbPufE.js";import{h as ne}from"../chunks/Bm5_blfa.js";import{C as B}from"../chunks/FdTAqBrw.js";import{T as se,a as h,b as _}from"../chunks/bBFAaf_v.js";import{D as v}from"../chunks/B0C8H7r1.js";import{M as re}from"../chunks/Ck1zUBrh.js";import{T as k}from"../chunks/BBMkE1cu.js";import{c as de}from"../chunks/CC8l6xUA.js";var ce=p(`<!> <table><thead><tr><th class="white-space:nowrap">Tag</th><th>Where it lands</th></tr></thead><tbody><tr><td><code>@param</code></td><td><!>.<code>description</code> on the matching
							parameter. Dotted keys (<code>obj.prop</code>) land in <code>propertyDescriptions</code>. Keys matching no parameter emit <code>unknown_param</code></td></tr><tr><td><code>@returns</code></td><td><code>returnDescription</code> on functions, function members, and per-overload <!>. <code>@return</code> (JSDoc synonym) parses identically</td></tr><tr><td><code>@throws</code></td><td><code>throws</code> array on the parent declaration. <code></code> hints are extracted
							as the leading error type</td></tr><tr><td><code>@example</code></td><td><code>examples</code> array on the parent declaration</td></tr><tr><td><code>@deprecated</code></td><td><code>deprecatedMessage</code> on the parent declaration. Empty body still marks the symbol
							deprecated</td></tr><tr><td><code>@internal</code></td><td><code>internalMessage</code> on the parent declaration or member. A marker, not an exclusion
							— the declaration stays documented; trailing prose is kept, and an empty body still marks
							the symbol internal</td></tr><tr><td><code>@see</code></td><td><code>seeAlso</code> array. Plain URLs, <code></code> syntax, and module names
							all preserved in their source form</td></tr><tr><td><code>@since</code></td><td><code>since</code> string on the parent declaration</td></tr><tr><td><code>@default</code></td><td><code>defaultValue</code> on variable declarations and on members of both kinds — for
							a function member it documents the behavior used when the callback is omitted; falls
							back for <!>.<code>defaultValue</code> when
							no destructuring default is present. Never lands on top-level function declarations or
							overloads. <code>@defaultValue</code> (TSDoc spelling) and <code>@defaultvalue</code> (JSDoc synonym) parse identically</td></tr><tr><td><code>@mutates</code></td><td>Non-standard tag for marking params that get mutated by a function. Uses the same
							format as <code>@param</code>: <code>@mutates key - description</code></td></tr><tr><td><code>@nodocs</code></td><td>Non-standard tag that excludes the declaration from output entirely; also excludes it
							from flat-namespace duplicate checking. Declaration- and statement-level only — in a <code>@module</code> comment it has no effect and warns</td></tr><tr><td><code>@module</code></td><td>Promotes the comment to <!>.<code>moduleComment</code> instead of attaching to a declaration</td></tr></tbody></table>`,1),ie=p(`<!> <p>Two tags vary per overload signature; the rest describe the symbol as a whole.</p> <ul><li><strong>Signature-scope</strong>: <code>@param</code> and <code>@returns</code>. These
					flow to the matching overload's <code>parameters[i].description</code> / <code>returnDescription</code>. Each overload can carry its own.</li> <li><strong>Symbol-scope</strong>: <code>@example</code>, <code>@deprecated</code>, <code>@internal</code>, <code>@since</code>, <code>@see</code>, <code>@throws</code>, <code>@mutates</code>, <code>@default</code>, <code>@nodocs</code>. These describe the
					symbol as a whole and live on the parent declaration only.</li></ul> <p>Placing a symbol-scope tag on a non-primary overload emits <code>misplaced_tag</code> and the tag is dropped, with no synthetic content and no silent
				loss. Move it to the primary signature (typically the first overload, or the implementation
				signature's JSDoc which feeds the symbol-level extraction). See <!> for the diagnostic details.</p> <!>`,1),le=p(`<!> <p><code>@param</code> keys are matched against actual parameter names. The leading <code>-</code> separator is stripped (TypeScript's parser keeps it as syntax, not content).
				A bare key matches the parameter name; a dotted key (<code>obj.prop</code>) documents a
				property of a named object parameter.</p> <p>Dotted keys whose root segment is a real parameter (<code>obj</code> in <code>obj.prop</code>) land in <!>'s <code>propertyDescriptions</code> record, keyed by the sub-path (<code>obj.prop</code> → <code>prop</code>, <code>obj.a.b</code> → <code>a.b</code>). The property segment is not
				validated against the parameter's type. Matching is by parameter name, so destructured
				parameters (<code></code>, which TypeScript names <code>__0</code>) are not
				covered.</p> <p>A key matching no parameter — or a dotted key whose root segment matches none — drops its
				description and fires an <code>unknown_param</code> diagnostic with the orphaned key, usually
				a typo or stale doc after a rename. Fix the JSDoc rather than relying on the silent fallback.</p>`,1),pe=p(`<!> <p>Non-standard tag for documenting mutations to parameters or external state. Same <code>key - description</code> format as <code>@param</code>, but keys are <strong>not validated</strong> against the parameter list. Anything goes:</p> <ul><li>a parameter name: <code>@mutates options - sets defaults in place</code></li> <li>a compound path: <code>@mutates this.cache - inserts the result</code></li> <li>an external state reference: <code>@mutates global_registry - registers the handler</code></li></ul> <p>The output is a <code>Record&lt;string, string&gt;</code> mapping each key to its description.
				Consumers decide how to render or group by key shape.</p>`,1),me=p(`<!> <p><code>@internal</code> marks a symbol as "not stable public API" per TSDoc semantics,
				landing as <code>internalMessage</code> on declarations and members of every kind. Presence
				means the tag was written — a bare tag yields an empty string, and trailing prose (common in
				JSDoc corpora: <code>@internal used during development</code>) is kept as the field's value,
				separate from <code>docComment</code>.</p> <p>It is a <strong>marker, not an exclusion</strong>: the declaration remains fully documented,
				so consumers can render a badge or filter as they see fit. To remove a declaration from
				output entirely, use <code>@nodocs</code>; to omit whole modules, use <code>exclude</code> patterns.</p>`,1),ue=p(`<!> <p><code>@nodocs</code> on a declaration removes it from the analysis output entirely. Two follow-on
				effects:</p> <ul><li><strong>Flat-namespace duplicate checking skips it</strong>: a hidden helper named <code>parse</code> can coexist with a public <code>parse</code> in another module without
					triggering <code>duplicate_declaration</code>.</li> <li><strong>Re-export synthesis is suppressed</strong>: <code>@nodocs</code> on a re-export
					statement drops both the <code>alsoExportedFrom</code> link and any synthesized alias declaration.
					The canonical entry stays untouched.</li> <li><strong>Merged value+type pairs honor it from either side</strong>: a <code>const Foo</code> + <code>type Foo</code> pair is one merged symbol, so the tag on either
					declaration excludes the pair's single declaration.</li></ul>`,1),he=p(`<!> <p>A comment tagged <code>@module</code> attaches to the file rather than to the next
				declaration. The text lands on <code>ModuleJson.moduleComment</code> and is suppressed from
				any declaration <code>docComment</code> that might otherwise capture it.</p> <!> <p>Svelte files have a second module-comment source: an HTML comment directly above <code>&lt;script&gt;</code>. When both an HTML and a JSDoc <code>@module</code> comment
				supply a value for the same target, <code>duplicate_comment</code> fires with <code>commentType: "module_comment"</code>. The same diagnostic covers declaration-level
				collisions (<code>commentType: "doc_comment"</code>).</p>`,1),_e=p(`<!> <p>A re-export that carries its own JSDoc synthesizes an alias in the re-exporting module so
				the local content has somewhere to live, even when the name is unchanged. See <!> for the full encoding rules and merge order.</p>`,1),ge=p(`<!> <p>Three diagnostic kinds surface tag-handling problems:</p> <ul><li><code>misplaced_tag</code>: symbol-scope tag on a non-primary overload signature, or <code>@nodocs</code> in a <code>@module</code> comment (no module-level meaning — use <code>exclude</code> patterns to omit a module)</li> <li><code>unknown_param</code>: <code>@param</code> key with no matching parameter</li> <li><code>duplicate_comment</code>: two sources supplied a comment for the same target (HTML +
					JSDoc <code>@module</code>, or any other collision)</li></ul> <p>All three are warnings; analysis still completes and the declaration is included. See <!> for the parser internals and <!> for the full diagnostic schema.</p>`,1),fe=p(`<section><p>svelte-docinfo extracts TSDoc/JSDoc tags from source comments and surfaces them as structured
			fields on the <!>. This page lists every tag that
			is parsed, where its value lands, and the rules that decide which symbol receives it. The set
			is the common doc tags of <a href="https://jsdoc.app/">JSDoc</a> and the <a href="https://tsdoc.org/">TSDoc spec</a> as used across the TypeScript ecosystem — where
			the two standards spell a tag differently (JSDoc <code>@default</code> vs TSDoc <code>@defaultValue</code>, JSDoc's <code>@return</code> synonym vs <code>@returns</code>),
			both spellings are accepted — plus the custom tags <code>@nodocs</code> and <code>@mutates</code>. Inline tags like <code></code> are not processed: they stay verbatim
			in the extracted text, and rendering them is the consumer's concern. Please submit issues if it's
			missing something you need or if any tags are off-spec.</p> <!> <!> <!> <!> <!> <!> <!> <!> <!></section>`);function Je(U,W){ee(W,!0);const Y=de("tags");ne("ri38dm",D=>{oe(()=>{ae.title="tags - svelte-docinfo"})}),se(U,{get tome(){return Y},children:(D,ye)=>{var b=fe(),$=a(b),P=e(a($));k(P,{slug:"output-format",children:(s,m)=>{r();var t=S("output");c(s,t)},$$slots:{default:!0}});var K=e(P,18);K.textContent="{@link}",r(),n($);var J=e($,2);h(J,{children:(s,m)=>{var t=ce(),o=u(t);_(o,{text:"Supported tags"});var d=e(o,2),i=e(a(d)),l=a(i),g=e(a(l)),y=a(g);v(y,{name:"ParameterJson"}),r(8),n(g),n(l);var f=e(l),I=e(a(f)),q=e(a(I),2);v(q,{name:"OverloadJson"}),r(3),n(I),n(f);var w=e(f),N=e(a(w)),G=e(a(N),2);G.textContent="{Type}",r(),n(N),n(w);var x=e(w,4),F=e(a(x)),Q=e(a(F),2);Q.textContent="{@link}",r(),n(F),n(x);var T=e(x,2),z=e(a(T)),X=e(a(z),2);v(X,{name:"ComponentPropJson"}),r(7),n(z),n(T);var H=e(T,3),V=e(a(H)),Z=e(a(V));v(Z,{name:"ModuleJson"}),r(3),n(V),n(H),n(i),n(d),c(s,t)},$$slots:{default:!0}});var M=e(J,2);h(M,{children:(s,m)=>{var t=ie(),o=u(t);_(o,{text:"Symbol-scope vs signature-scope"});var d=e(o,6),i=e(a(d),3);k(i,{slug:"diagnostics"}),r(),n(d);var l=e(d,2);B(l,{lang:"ts",dangerous_raw_html:`<span class="token_comment">/**
 * @example double(2) // 4
 * @deprecated use \`scale\` instead
 */</span>
<span class="token_special_keyword">export</span> <span class="token_keyword">function</span> <span class="token_function">double</span><span class="token_punctuation">(</span>n<span class="token_operator">:</span> <span class="token_builtin">number</span><span class="token_punctuation">)</span><span class="token_operator">:</span> <span class="token_builtin">number</span><span class="token_punctuation">;</span>
<span class="token_special_keyword">export</span> <span class="token_keyword">function</span> <span class="token_function">double</span><span class="token_punctuation">(</span>n<span class="token_operator">:</span> bigint<span class="token_punctuation">)</span><span class="token_operator">:</span> bigint<span class="token_punctuation">;</span>
<span class="token_special_keyword">export</span> <span class="token_keyword">function</span> <span class="token_function">double</span><span class="token_punctuation">(</span>n<span class="token_operator">:</span> <span class="token_builtin">number</span> <span class="token_operator">|</span> bigint<span class="token_punctuation">)</span><span class="token_operator">:</span> <span class="token_builtin">number</span> <span class="token_operator">|</span> bigint <span class="token_punctuation">{</span>
  <span class="token_special_keyword">return</span> <span class="token_punctuation">(</span>n <span class="token_special_keyword">as</span> <span class="token_builtin">any</span><span class="token_punctuation">)</span> <span class="token_operator">*</span> <span class="token_number">2</span><span class="token_punctuation">;</span>
<span class="token_punctuation">}</span>`}),c(s,t)},$$slots:{default:!0}});var A=e(M,2);h(A,{children:(s,m)=>{var t=le(),o=u(t);_(o,{text:"@param matching and unknown_param"});var d=e(o,4),i=e(a(d),5);v(i,{name:"ParameterJson"});var l=e(i,12);l.textContent="fn({a, b}: T)",r(3),n(d),r(2),c(s,t)},$$slots:{default:!0}});var C=e(A,2);h(C,{children:(s,m)=>{var t=pe(),o=u(t);_(o,{text:"@mutates"}),r(6),c(s,t)},$$slots:{default:!0}});var L=e(C,2);h(L,{children:(s,m)=>{var t=me(),o=u(t);_(o,{text:"@internal"}),r(4),c(s,t)},$$slots:{default:!0}});var R=e(L,2);h(R,{children:(s,m)=>{var t=ue(),o=u(t);_(o,{text:"@nodocs"}),r(4),c(s,t)},$$slots:{default:!0}});var j=e(R,2);h(j,{children:(s,m)=>{var t=he(),o=u(t);_(o,{text:"@module"});var d=e(o,4);B(d,{lang:"ts",dangerous_raw_html:`<span class="token_comment">/**
 * Date math utilities.
 *
 * @module
 */</span>

<span class="token_special_keyword">export</span> <span class="token_keyword">function</span> <span class="token_function">add_days</span><span class="token_punctuation">(</span>d<span class="token_operator">:</span> <span class="token_capitalized_identifier token_class_name">Date</span><span class="token_punctuation">,</span> n<span class="token_operator">:</span> <span class="token_builtin">number</span><span class="token_punctuation">)</span><span class="token_operator">:</span> <span class="token_capitalized_identifier token_class_name">Date</span> <span class="token_punctuation">{</span> <span class="token_comment">/* ... */</span> <span class="token_punctuation">}</span>`}),r(2),c(s,t)},$$slots:{default:!0}});var E=e(j,2);h(E,{children:(s,m)=>{var t=_e(),o=u(t);_(o,{text:"Re-exports inherit comments selectively"});var d=e(o,2),i=e(a(d));k(i,{slug:"output-format",children:(l,g)=>{r();var y=S("Re-exports");c(l,y)},$$slots:{default:!0}}),r(),n(d),c(s,t)},$$slots:{default:!0}});var O=e(E,2);h(O,{children:(s,m)=>{var t=ge(),o=u(t);_(o,{text:"Tag-related diagnostics"});var d=e(o,6),i=e(a(d));re(i,{module_path:"tsdoc.ts",children:(g,y)=>{r();var f=S("tsdoc.ts");c(g,f)},$$slots:{default:!0}});var l=e(i,2);k(l,{slug:"diagnostics"}),r(),n(d),c(s,t)},$$slots:{default:!0}}),n(b),c(D,b)},$$slots:{default:!0}}),te()}export{Je as component};
