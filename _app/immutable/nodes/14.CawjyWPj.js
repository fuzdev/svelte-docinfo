import"../chunks/DsnmJJEf.js";import{p as j,b as T,i as S,f as m,a as d,s as t,g as N,c,d as g,$ as O,n as r,r as f}from"../chunks/iYPbPufE.js";import{h as q}from"../chunks/Bm5_blfa.js";import{C as b}from"../chunks/FdTAqBrw.js";import{T as C,a as w,b as v}from"../chunks/bBFAaf_v.js";import{T as E}from"../chunks/BBMkE1cu.js";import{c as I}from"../chunks/CC8l6xUA.js";var J=c(`<!> <!> <p>Discovery defaults to <code>package.json</code> exports, falling back to glob. <code>-i</code> forces explicit patterns; <code>--discovery glob</code> skips exports; <code>--discovery exports</code> is strict (fails when exports is missing).</p> <p><code>--source-dir</code> sets the source directory (default <code>src/lib</code>,
				repeatable for monorepos) and seeds the implicit include glob. <code>--source-root</code> controls module-path stripping in the output (defaults to the
				single <code>--source-dir</code> or their longest common prefix).</p> <p>Compact JSON pairs well with <code>jq</code>:</p> <!> <p>JSON goes to stdout; info, warnings, and errors go to stderr, so the terminal interleaves
				them, but <code>&gt;</code> and <code>|</code> capture clean JSON. <code>-q</code>/<code>--quiet</code> silences info; warnings and errors still print.</p>`,1),R=c(`<!> <table><thead><tr><th class="white-space:nowrap">Flag</th><th>Description</th></tr></thead><tbody><tr><td><code>[project-root]</code></td><td>project root directory (default: cwd)</td></tr><tr><td class="white-space:nowrap"><code>-i, --include &lt;pattern&gt;</code></td><td>include pattern (repeatable, replaces exports discovery, widens the source scope — see
							the note below)</td></tr><tr><td class="white-space:nowrap"><code>-e, --exclude &lt;pattern&gt;</code></td><td>exclude glob, applied at discovery and analysis (repeatable; fully replaces the
							defaults <code>**/*.test.ts</code>, <code>**/*.spec.ts</code>, <code>**/internal/**</code> — no merge; the always-on baseline below applies beneath)</td></tr><tr><td class="white-space:nowrap"><code>-o, --output &lt;file&gt;</code></td><td>output file (default: stdout; pass <code>-</code> for explicit stdout, so <code>-o "$OUT"</code> works when <code>$OUT=-</code>)</td></tr><tr><td class="white-space:nowrap"><code>--discovery &lt;mode&gt;</code></td><td><code>auto</code> | <code>exports</code> | <code>glob</code> (default: <code>auto</code>: exports first, glob fallback). <code>exports</code> is strict and fails
							when package.json exports is missing.</td></tr><tr><td class="white-space:nowrap"><code>--dist-dir &lt;dir&gt;</code></td><td>dist directory for exports discovery (default: dist)</td></tr><tr><td class="white-space:nowrap"><code>--source-dir &lt;dir&gt;</code></td><td>source directory, relative to project root or absolute inside it (default: src/lib).
							Repeatable for monorepos; also seeds the implicit include glob.</td></tr><tr><td class="white-space:nowrap"><code>--source-root &lt;dir&gt;</code></td><td>source root for module-path stripping (default: single source-dir or longest common
							prefix)</td></tr><tr><td class="white-space:nowrap"><code>--on-duplicates &lt;mode&gt;</code></td><td>dispatch on duplicate declaration names: <code>throw</code> | <code>warn</code> (default: emit <code>duplicate_declaration</code> diagnostic, no dispatch)</td></tr><tr><td class="white-space:nowrap"><code>--only &lt;pattern&gt;</code></td><td>glob filter applied to module paths in output (repeatable). Full project is still
							analyzed (re-exports/dependents stay correct); diagnostics aren't filtered</td></tr><tr><td><code>--no-resolve-dependencies</code></td><td>disable dependency resolution</td></tr><tr><td><code>--pretty</code></td><td>pretty-print JSON output (default: compact)</td></tr><tr><td><code>-q, --quiet</code></td><td>suppress info messages on stderr (warnings and errors still print)</td></tr><tr><td><code>-V, --version</code></td><td>show version number</td></tr></tbody></table> <p>Explicit <code>--include</code> patterns widen the source scope: each pattern's static base
				joins the source paths, so module paths become relative to the widened root, and a pattern
				with no base (<code>**/*.ts</code>, a literal root file) scopes the whole project root as
				source and logs an info line. Beneath any <code>--exclude</code>, an always-on baseline
				applies: <code>node_modules</code> and dot-directories below a source dir are never source.
				The default excludes cover tests and the <code>src/lib/internal/</code> convention (<code>**/internal/**</code>); exports-based discovery additionally honors <code>null</code>-target exports keys (<code>"./internal/*": null</code>) with Node's
				best-match resolution, so blocked subpaths are never discovered. Absolute paths and patterns
				inside the project root are accepted (relativized); out-of-root ones fail loudly instead of
				silently emitting nothing.</p> <p>Exit codes: <strong>0</strong> success, <strong>1</strong> analysis errors, <strong>2</strong> CLI errors.</p>`,1),A=c(`<section><p>Run in any directory with TypeScript or Svelte source files. Prints JSON describing your
			project's exports to stdout.</p> <!> <!> <p>For SvelteKit and Vite projects where the analysis feeds into your app bundle, see the <!>.</p></section>`);function Y(y,x){j(x,!0);const _=I("cli");q("y492lb",n=>{S(()=>{O.title="CLI - svelte-docinfo"})}),C(y,{get tome(){return _},children:(n,P)=>{var a=A(),i=t(g(a),2);w(i,{children:(o,u)=>{var e=J(),s=m(e);v(s,{text:"Basic usage"});var h=t(s,2);b(h,{lang:"bash",dangerous_raw_html:`npx svelte-docinfo                    <span class="token_comment"># analyze the current directory</span>
npx svelte-docinfo ./packages/my-lib  <span class="token_comment"># analyze a specific directory</span>
npx svelte-docinfo -o output.json     <span class="token_comment"># write to a file instead</span>
npx svelte-docinfo --pretty           <span class="token_comment"># pretty-print the JSON output</span>`});var k=t(h,8);b(k,{lang:"bash",dangerous_raw_html:`npx svelte-docinfo <span class="token_operator">|</span> jq <span class="token_string">'.modules | length'</span>                  <span class="token_comment"># count modules</span>
npx svelte-docinfo <span class="token_operator">|</span> jq -r <span class="token_string">'.modules[].declarations[].name'</span>  <span class="token_comment"># list all exported names</span>`}),r(2),d(o,e)},$$slots:{default:!0}});var l=t(i,2);w(l,{children:(o,u)=>{var e=R(),s=m(e);v(s,{text:"Options"}),r(6),d(o,e)},$$slots:{default:!0}});var p=t(l,2),$=t(g(p));E($,{slug:"vite-plugin",children:(o,u)=>{r();var e=N("Vite plugin");d(o,e)},$$slots:{default:!0}}),r(),f(p),f(a),d(n,a)},$$slots:{default:!0}}),T()}export{Y as component};
