import"../chunks/DsnmJJEf.js";import{p as B,c as Y,g as Q,f as m,a as h,d as t,s,b as d,ae as M,ah as j,al as c,r as o}from"../chunks/DvBAEzb_.js";import{h as G}from"../chunks/CJebTnMK.js";import{C as w}from"../chunks/CmzOoWZv.js";import{T as K,a as f,b as v}from"../chunks/CAaM58hF.js";import{D as l}from"../chunks/CAk6ul17.js";import{M as X}from"../chunks/ZLCzvoTm.js";import{T as E}from"../chunks/BfD4u3bY.js";import{c as Z}from"../chunks/Bmjj7vR7.js";var ss=m(`<!> <!> <p><!> requires a fully-constructed <code>ModuleSourceOptions</code>. Use <!> to merge with <!>. The session re-runs <code>normalizeSourceOptions</code> (idempotent) but does not apply any further defaults.</p> <p><code>tsconfig</code> and <code>compilerOptions</code> flow to both the underlying <code>LanguageService</code> and the lazy default <!>.
			User-supplied <code>compilerOptions</code> merge over the parsed tsconfig per key, but the tsconfig
			file is still required.</p>`,1),ns=m(`<!> <p>The session exposes a small incremental surface — add, update, remove — for owning the source
			set:</p> <table><thead><tr><th class="white-space:nowrap">Method</th><th>Purpose</th></tr></thead><tbody><tr><td class="white-space:nowrap"><code>setFile(file, opts?)</code></td><td>Ingest one file's content. Idempotent on cache hit. Returns <!></td></tr><tr><td class="white-space:nowrap"><code>setFiles(files, opts?)</code></td><td>Ingest a batch. Additive, never removes. Returns <!> with both aggregate and per-file views</td></tr><tr><td class="white-space:nowrap"><code>deleteFile(id)</code></td><td>Drop a file from the session and evict it from the LS</td></tr><tr><td class="white-space:nowrap"><code>has(id)</code></td><td>Whether the given absolute path is currently owned</td></tr><tr><td class="white-space:nowrap"><code>list()</code></td><td>Snapshot of owned file IDs (insertion order)</td></tr><tr><td class="white-space:nowrap"><code>query(opts?)</code></td><td>Run a two-phase analysis pass against the current owned set. Returns <!></td></tr><tr><td class="white-space:nowrap"><code>allIngestDiagnostics()</code></td><td>Cumulative ingest-time diagnostics across every owned entry</td></tr><tr><td class="white-space:nowrap"><code>dispose()</code></td><td>Release LS resources and clear the owned set. Session must not be used after</td></tr></tbody></table> <p><strong>Concurrency.</strong> The session is not safe across overlapping calls, so serialize
			externally. The LS underneath is sync, but the resolver phase awaits I/O for async resolvers
			(Vite/Rollup), so <code>setFile</code> / <code>setFiles</code> cross await boundaries. <code>query</code> is sync and reads the current owned set; await any pending ingest first.</p>`,1),es=m(`<!> <p>Each owned entry is all-or-nothing per cache decision: either every cached artifact for that
			file is reused, or every artifact is recomputed. Whether a re-ingest cache-hits is
			mode-discriminated:</p> <ul><li><strong>lex+resolve</strong> (default): cache key is <code>(content, resolverIdentity)</code>. Match requires byte-for-byte content equality AND
				identity equality on the <!>.</li> <li><strong>pre-resolved</strong> (caller supplies <code>SourceFileInfo.dependencies</code>):
				cache key is <code>(content, dependencies-element-wise-equal)</code>. A fresh array with
				identical contents cache-hits (so <code>[...filer.deps.keys()]</code>-per-call works); any
				length, element, or order difference cache-misses.</li></ul> <p>A mode flip (an entry previously ingested as lex+resolve now arrives with <code>dependencies</code>, or vice versa) always cache-misses and rewrites the entry.</p> <p>On a cache hit, <code>SetFileResult.changed</code> is <code>false</code> and the cached ingest diagnostics
			are returned with no work run. On a miss, the entry is rewritten. The LS push happens on miss for
			TS/JS files and for Svelte files with a successful svelte2tsx virtual; CSS/JSON and transform-failed
			Svelte rewrite the entry without touching the LS.</p>`,1),as=m(`<!> <p><!> is a token pair: <code></code>. <code>identity</code> is a stable opaque token (string or
			symbol) that keys the resolve cache alongside content.</p> <!> <p>Identity is required (not optional). The naive alternative, keying on the function reference,
			would silently destroy cache reuse when callers wrap the resolver in a fresh closure per call
			(a common Vite/Rollup pattern). Opaque tokens lift the responsibility to the caller, where it
			can be done correctly: reuse the same string/symbol across calls if the resolution semantics
			haven't changed.</p> <p>When neither <code>AnalysisSessionOptions.resolveImport</code> nor a per-call <!>.<code>resolveImport</code> is supplied, the session
			lazily constructs a TS + tsconfig default with a fresh symbol identity on first use. That
			laziness matters: if every file in every batch arrives fully pre-resolved (<code>SourceFileInfo.dependencies</code> populated), the default is never built and <code>loadTsconfig</code> is never called, saving a multi-second <code>ts.createProgram</code> on cold start.</p>`,1),ts=m(`<!> <p>When a caller supplies <code>SourceFileInfo.dependencies</code>, the session accepts the array
			unconditionally and skips its own resolve pass. A buggy caller-side resolver skews <code>ModuleJson.dependencies</code> / <code>dependents</code> with no warning. The
			lex+resolve fallback is always grounded in syntactic imports, so switch to it if you don't
			control the dependency source. See <!> for the full trust contract and type-only-edge
			policy.</p>`,1),os=m(`<!> <p>Diagnostic kinds split into two categories with different lifecycles:</p> <ul><li><strong>Ingest-time</strong> (<code>transform_failed</code>, <code>source_map_failed</code>, <code>import_parse_failed</code>, <code>resolver_failed</code>): surfaced via <code>setFile</code> / <code>setFiles</code> returns and durable on the owned entry. Survive subsequent <code>query</code> calls until the
				entry is replaced or deleted.</li> <li><strong>Query-time</strong> (the rest): recomputed on every <code>query</code> call.
				Returned in <code>AnalyzeResultJson.diagnostics</code>.</li></ul> <p><code>query()</code> returns analysis-pass diagnostics only; it does NOT include ingest
			diagnostics. Concat with prior <code>setFile</code> / <code>setFiles</code> returns for the
			full picture, or call <code>allIngestDiagnostics()</code> for the cumulative ingest view across
			every owned entry:</p> <!> <p><code>allIngestDiagnostics</code> is the publish path for long-lived consumers. It lets the Vite
			plugin republish the cumulative ingest picture on every HMR cycle without tracking per-batch returns,
			and lets an LSP push a complete diagnostic set to the client on demand. Cheap: walks the owned map.</p> <p>Discovery-time diagnostics (<code>module_unreadable</code> from <code>discoverFromExports</code>) are a third category. The session doesn't run discovery, so
			direct consumers own those: track them in a side-channel field for HMR survival as the Vite
			plugin does, or run discovery once and merge into the first query.</p>`,1),ps=m(`<!> <p>Sketch of an LSP-style edit/save/query loop. Each <code>setFile</code> updates a single file; <code>query</code> reanalyzes the whole owned set but reuses parsed ASTs and svelte2tsx output for
			unchanged files.</p> <!> <p>For HMR / file-watcher consumers, <code>setFiles</code> on the batch of changed paths in one
			call is preferable to looping <code>setFile</code>: it shares the <code>ensureLexerReady</code> warmup and runs the resolve phase in parallel with a bounded worker
			pool.</p>`,1),cs=m(`<!> <p>If you only call analysis once (CLI, CI pipeline, one-off doc generation), use <!> or <!> directly. They create a single-use session, run it,
			and dispose. There's no caching benefit to keeping a session alive across one call. See <!> for the full type definitions and <!> for the diagnostic kinds.</p>`,1),rs=m(`<section><p><!> returns a persistent analysis handle backed by
			a TypeScript <code>LanguageService</code>. Use it when the same source set is re-analyzed
			repeatedly (Vite plugin, LSP-style tools) so parsed ASTs, svelte2tsx output, and the
			dependency graph are reused across cycles. The one-shot <!> and <!> are thin wrappers over single-use sessions.</p></section> <!> <!> <!> <!> <!> <!> <!> <!>`,1);function gs(N,W){B(W,!0);const J=Z("session");G("ihg8at",$=>{Q(()=>{j.title="session - svelte-docinfo"})}),K(N,{get tome(){return J},children:($,ls)=>{var I=rs(),b=h(I),R=t(b),T=t(R);l(T,{name:"createAnalysisSession"});var F=s(T,4);l(F,{name:"analyze"});var U=s(F,2);l(U,{name:"analyzeFromFiles"}),c(),o(R),o(b);var x=s(b,2);f(x,{children:(r,y)=>{var n=ss(),e=h(n);v(e,{text:"Construction"});var a=s(e,2);w(a,{lang:"ts",dangerous_raw_html:`<span class="token_special_keyword">import</span> <span class="token_punctuation">{</span>createAnalysisSession<span class="token_punctuation">,</span> createSourceOptions<span class="token_punctuation">}</span> <span class="token_special_keyword">from</span> <span class="token_string">'svelte-docinfo'</span><span class="token_punctuation">;</span>

<span class="token_keyword">const</span> session <span class="token_operator">=</span> <span class="token_function">createAnalysisSession</span><span class="token_punctuation">(</span><span class="token_punctuation">{</span>
  sourceOptions<span class="token_operator">:</span> <span class="token_function">createSourceOptions</span><span class="token_punctuation">(</span>process<span class="token_punctuation">.</span><span class="token_function">cwd</span><span class="token_punctuation">(</span><span class="token_punctuation">)</span><span class="token_punctuation">)</span><span class="token_punctuation">,</span>
  <span class="token_comment">// Optional: session-default ImportResolver. Lazily constructed</span>
  <span class="token_comment">// (TS + tsconfig) on first use when omitted, unless every batch</span>
  <span class="token_comment">// arrives fully pre-resolved.</span>
  <span class="token_comment">// resolveImport: myResolver,</span>
  <span class="token_comment">// Optional: logger for session-level messages.</span>
  <span class="token_comment">// log: console,</span>
<span class="token_punctuation">}</span><span class="token_punctuation">)</span><span class="token_punctuation">;</span>`});var p=s(a,2),i=t(p);l(i,{name:"AnalysisSessionOptions"});var u=s(i,4);l(u,{name:"createSourceOptions"});var _=s(u,2);l(_,{name:"DEFAULT_SOURCE_OPTIONS"}),c(3),o(p);var k=s(p,2),g=s(t(k),6);l(g,{name:"ImportResolver"}),c(3),o(k),d(r,n)},$$slots:{default:!0}});var O=s(x,2);f(O,{children:(r,y)=>{var n=ns(),e=h(n);v(e,{text:"Lifecycle"});var a=s(e,4),p=s(t(a)),i=t(p),u=s(t(i)),_=s(t(u));l(_,{name:"SetFileResult"}),o(u),o(i);var k=s(i),g=s(t(k)),S=s(t(g));l(S,{name:"SetFilesResult"}),c(),o(g),o(k);var D=s(k,4),C=s(t(D)),H=s(t(C));l(H,{name:"AnalyzeResultJson"}),o(C),o(D),c(2),o(p),o(a),c(2),d(r,n)},$$slots:{default:!0}});var q=s(O,2);f(q,{children:(r,y)=>{var n=es(),e=h(n);v(e,{text:"Cache-hit semantics"});var a=s(e,4),p=t(a),i=s(t(p),4);l(i,{name:"ImportResolver"}),c(),o(p),c(2),o(a),c(4),d(r,n)},$$slots:{default:!0}});var A=s(q,2);f(A,{children:(r,y)=>{var n=as(),e=h(n);v(e,{text:"ImportResolver and identity"});var a=s(e,2),p=t(a);l(p,{name:"ImportResolver"});var i=s(p,2);i.textContent="{resolve, identity}",c(3),o(a);var u=s(a,2);w(u,{lang:"ts",dangerous_raw_html:`<span class="token_special_keyword">import</span> <span class="token_keyword">type</span> <span class="token_punctuation">{</span><span class="token_capitalized_identifier token_class_name">ImportResolver</span><span class="token_punctuation">}</span> <span class="token_special_keyword">from</span> <span class="token_string">'svelte-docinfo'</span><span class="token_punctuation">;</span>

<span class="token_keyword">const</span> myResolver<span class="token_type_annotation"><span class="token_:">:</span><span class="token_type"> <span class="token_capitalized_identifier token_class_name">ImportResolver</span> </span></span><span class="token_operator">=</span> <span class="token_punctuation">{</span>
  identity<span class="token_operator">:</span> <span class="token_string">'vite-plugin-container'</span><span class="token_punctuation">,</span>
  <span class="token_function_variable token_function">resolve</span><span class="token_operator">:</span> <span class="token_punctuation">(</span>specifier<span class="token_punctuation">,</span> fromFile<span class="token_punctuation">)</span> <span class="token_operator">=></span> <span class="token_punctuation">{</span>
    <span class="token_comment">// return absolute path, or null for externals</span>
    <span class="token_special_keyword">return</span> <span class="token_keyword">null</span><span class="token_punctuation">;</span>
  <span class="token_punctuation">}</span><span class="token_punctuation">,</span>
<span class="token_punctuation">}</span><span class="token_punctuation">;</span>`});var _=s(u,4),k=s(t(_),3);l(k,{name:"SetFileOptions"}),c(9),o(_),d(r,n)},$$slots:{default:!0}});var L=s(A,2);f(L,{children:(r,y)=>{var n=ts(),e=h(n);v(e,{text:"Trust mode for pre-resolved dependencies"});var a=s(e,2),p=s(t(a),7);E(p,{slug:"build-tools",children:(i,u)=>{c();var _=M("build-tool integration");d(i,_)},$$slots:{default:!0}}),c(),o(a),d(r,n)},$$slots:{default:!0}});var P=s(L,2);f(P,{children:(r,y)=>{var n=os(),e=h(n);v(e,{text:"Diagnostics: ingest-time vs query-time"});var a=s(e,8);w(a,{lang:"ts",dangerous_raw_html:`<span class="token_keyword">const</span> queryResult <span class="token_operator">=</span> session<span class="token_punctuation">.</span><span class="token_function">query</span><span class="token_punctuation">(</span><span class="token_punctuation">)</span><span class="token_punctuation">;</span>
<span class="token_keyword">const</span> fullDiagnostics <span class="token_operator">=</span> <span class="token_punctuation">[</span>
  <span class="token_operator">...</span>session<span class="token_punctuation">.</span><span class="token_function">allIngestDiagnostics</span><span class="token_punctuation">(</span><span class="token_punctuation">)</span><span class="token_punctuation">,</span>
  <span class="token_operator">...</span>queryResult<span class="token_punctuation">.</span>diagnostics<span class="token_punctuation">,</span>
<span class="token_punctuation">]</span><span class="token_punctuation">;</span>`}),c(4),d(r,n)},$$slots:{default:!0}});var z=s(P,2);f(z,{children:(r,y)=>{var n=ps(),e=h(n);v(e,{text:"Worked example: incremental loop"});var a=s(e,4);w(a,{lang:"ts",dangerous_raw_html:`<span class="token_special_keyword">import</span> <span class="token_punctuation">{</span>createAnalysisSession<span class="token_punctuation">,</span> createSourceOptions<span class="token_punctuation">,</span> hasErrors<span class="token_punctuation">}</span> <span class="token_special_keyword">from</span> <span class="token_string">'svelte-docinfo'</span><span class="token_punctuation">;</span>

<span class="token_keyword">const</span> session <span class="token_operator">=</span> <span class="token_function">createAnalysisSession</span><span class="token_punctuation">(</span><span class="token_punctuation">{</span>
  sourceOptions<span class="token_operator">:</span> <span class="token_function">createSourceOptions</span><span class="token_punctuation">(</span>process<span class="token_punctuation">.</span><span class="token_function">cwd</span><span class="token_punctuation">(</span><span class="token_punctuation">)</span><span class="token_punctuation">)</span><span class="token_punctuation">,</span>
<span class="token_punctuation">}</span><span class="token_punctuation">)</span><span class="token_punctuation">;</span>

<span class="token_comment">// Initial population.</span>
<span class="token_special_keyword">await</span> session<span class="token_punctuation">.</span><span class="token_function">setFiles</span><span class="token_punctuation">(</span><span class="token_punctuation">[</span>
  <span class="token_punctuation">{</span>id<span class="token_operator">:</span> <span class="token_string">'/abs/src/lib/a.ts'</span><span class="token_punctuation">,</span> content<span class="token_operator">:</span> <span class="token_string">'...'</span><span class="token_punctuation">}</span><span class="token_punctuation">,</span>
  <span class="token_punctuation">{</span>id<span class="token_operator">:</span> <span class="token_string">'/abs/src/lib/b.ts'</span><span class="token_punctuation">,</span> content<span class="token_operator">:</span> <span class="token_string">'...'</span><span class="token_punctuation">}</span><span class="token_punctuation">,</span>
<span class="token_punctuation">]</span><span class="token_punctuation">)</span><span class="token_punctuation">;</span>

<span class="token_comment">// Edit: one file changed.</span>
<span class="token_keyword">const</span> <span class="token_punctuation">{</span>changed<span class="token_punctuation">,</span> diagnostics<span class="token_operator">:</span> ingest<span class="token_punctuation">}</span> <span class="token_operator">=</span> <span class="token_special_keyword">await</span> session<span class="token_punctuation">.</span><span class="token_function">setFile</span><span class="token_punctuation">(</span><span class="token_punctuation">{</span>
  id<span class="token_operator">:</span> <span class="token_string">'/abs/src/lib/a.ts'</span><span class="token_punctuation">,</span>
  content<span class="token_operator">:</span> <span class="token_string">'... new content ...'</span><span class="token_punctuation">,</span>
<span class="token_punctuation">}</span><span class="token_punctuation">)</span><span class="token_punctuation">;</span>

<span class="token_special_keyword">if</span> <span class="token_punctuation">(</span>changed<span class="token_punctuation">)</span> <span class="token_punctuation">{</span>
  <span class="token_keyword">const</span> <span class="token_punctuation">{</span>modules<span class="token_punctuation">,</span> diagnostics<span class="token_operator">:</span> pass<span class="token_punctuation">}</span> <span class="token_operator">=</span> session<span class="token_punctuation">.</span><span class="token_function">query</span><span class="token_punctuation">(</span><span class="token_punctuation">)</span><span class="token_punctuation">;</span>
  <span class="token_keyword">const</span> all <span class="token_operator">=</span> <span class="token_punctuation">[</span><span class="token_operator">...</span>session<span class="token_punctuation">.</span><span class="token_function">allIngestDiagnostics</span><span class="token_punctuation">(</span><span class="token_punctuation">)</span><span class="token_punctuation">,</span> <span class="token_operator">...</span>pass<span class="token_punctuation">]</span><span class="token_punctuation">;</span>
  <span class="token_special_keyword">if</span> <span class="token_punctuation">(</span><span class="token_function">hasErrors</span><span class="token_punctuation">(</span>all<span class="token_punctuation">)</span><span class="token_punctuation">)</span> <span class="token_punctuation">{</span>
    <span class="token_comment">// surface to the editor</span>
  <span class="token_punctuation">}</span>
<span class="token_punctuation">}</span>

<span class="token_comment">// Tear down.</span>
session<span class="token_punctuation">.</span><span class="token_function">dispose</span><span class="token_punctuation">(</span><span class="token_punctuation">)</span><span class="token_punctuation">;</span>`}),c(2),d(r,n)},$$slots:{default:!0}});var V=s(z,2);f(V,{children:(r,y)=>{var n=cs(),e=h(n);v(e,{text:"When to use one-shot APIs instead"});var a=s(e,2),p=s(t(a));l(p,{name:"analyze"});var i=s(p,2);l(i,{name:"analyzeFromFiles"});var u=s(i,2);X(u,{module_path:"session.ts",children:(k,g)=>{c();var S=M("session.ts");d(k,S)},$$slots:{default:!0}});var _=s(u,2);E(_,{slug:"diagnostics"}),c(),o(a),d(r,n)},$$slots:{default:!0}}),d($,I)},$$slots:{default:!0}}),Y()}export{gs as component};
